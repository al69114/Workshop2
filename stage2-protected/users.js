// Same users and passwords as Stage 1 — so the wordlist still works.
// The point: even with identical credentials, protections stop the attack.
const users = {
  alice: "sunshine99",
  bob: "password123",
  carol: "c@r0l2024",
  dave: "qwerty",
};

module.exports = users;
